#include <stdio.h>

int main(int argc, char *argv[])
{
	puts("Hello world.");
	puts("This needs to more complicated then hello world.");
	puts("Extra credit saids I need 5 more lines.");
	puts("Hmm this should work \" right? Somehow using single quotes gave me a Segmentation fault whatever that means.");
	puts("Turns out a segmentation fault (often shortened to segfault) or access violation is a fault raised by hardware with memory protection, notifying an operating system (OS) about a memory access violation; on x86 computers this is a form of general protection fault.");
	puts("Hopefully escape sequences \n\t\"work\".");

	return 0;
}