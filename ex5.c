#include <stdio.h>

/* This is a comment. 
	@params argc is an int for the argument count
			*argv is an array of char * strings
*/
int main(int argc, char *argv[])
{
	int distance = 100;

	// this is also a comment
	printf("You are %d miles away.\n", distance);

	return 0;
}